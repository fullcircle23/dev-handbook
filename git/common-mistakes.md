# Common Git Mistakes

- Forgetting to pull before push → merge conflicts.
- Amending commits after pushing → history rewrite issues.
- Using `reset --hard` without checking changes.
- Not using `.gitignore` properly.
